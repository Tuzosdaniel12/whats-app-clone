import styled from "styled-components";

const ChatScreen = () => {
	return <Container>
        <p>this is a chat</p>
    </Container>;
};

export default ChatScreen;

const Container = styled.div`

`